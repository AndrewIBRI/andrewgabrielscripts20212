# Aqui deveria ser os dias 12 ou 11 de Outubro. De onde você tirou esses horários?
# A versão passada da disciplina eu pedi isso. Então a conclusão é que você pegou as respostas de alguém do semestre passado.
$1 ~ /Dec/ && $2 ~ /4/ && ($3 > "18:00:00" && $3 < "19:00:00") && $8 ~ /opened/  {print }
